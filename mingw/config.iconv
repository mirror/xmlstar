#!/bin/sh
tail -n +3 "$0"
./configure \
    --prefix=/usr/local/mingw32 \
    --host=i586-mingw32msvc \
    --with-gnu-ld \
    --enable-silent-rules \
    \
    --enable-static \
    --disable-shared
